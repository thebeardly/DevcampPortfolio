

<div class="album py-5 bg-light">
  <div class="container">
    <%= link_to "Add New Portfolio Item", new_portfolio_path, class: 'btn btn-secondary' if logged_in?(:site_admin) %>
    <div class="row" data-controller="drag" data-drag-url="/portfolios/:id/move">
      <%= render partial: 'portfolio_item', collection: @portfolio_items %>
    </div>
  </div>
</div>